{
    "name": "prince-gupta-portfolio",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "build": "npm run build --prefix client",
        "install": "npm install --prefix client",
        "vercel-build": "npm install --prefix client && npm run build --prefix client"
    }
}