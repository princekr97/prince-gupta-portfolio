/**
 * Placeholder component.
 * The 3D cube experiment was removed to keep the bundle and runtime lean,
 * but the import is kept for older Hero variants.
 */
export const LiquidCube = (_props: { mouseX?: unknown; mouseY?: unknown }) => {
  return null;
};

